# Local environment variables (copy to `.env.local` — gitignored)
#
# Google OAuth Web client ID for Drive connect in local dev.
# See docs/features/cloud-storage/google-drive/README.md
# GOOGLE_OAUTH_CLIENT_ID=

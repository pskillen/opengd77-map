# Zone-derived scan lists — outstanding

Debt discovered during execution — not the plan backlog.

- **Import of synthetic rows** — scratch and scan carrier channels from DM32 export are not recognised on re-import; merge-candidate UI deferred.
- **Manual `ScanList` CRUD** — [#125](https://github.com/pskillen/codeplug-tool/issues/125) remains separate from zone-derived automation.
- **`Scan.csv` import** — export-only in v1; full round-trip waits on #125 or dedicated import slice.
